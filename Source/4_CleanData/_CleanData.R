print("Cleaning the data...");



# Write the cleaned data sets to disk
# write.csv(train, paste(parameters$folders$output, parameters$files$trainCleaned, sep="/"), row.names=FALSE);
# write.csv(valid, paste(parameters$folders$output, parameters$files$validCleaned, sep="/"), row.names=FALSE);
# write.csv(test,  paste(parameters$folders$output, parameters$files$testCleaned,  sep="/"), row.names=FALSE);
# write.csv(total, paste(parameters$folders$output, parameters$files$totalCleaned, sep="/"), row.names=FALSE);
